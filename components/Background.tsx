import React from 'react';

/**
 * Background component - now returns null as tree images have been removed.
 * The Starfield component provides the animated background.
 */
const Background: React.FC = () => null;

export default React.memo(Background);
